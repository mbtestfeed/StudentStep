<!-- replace partial with blank -->
console.log("<%= @entry.task.id %>");
$('#task<%= @entry.task.id %>')
    .replaceWith("<%= escape_javascript render('plans/completed_task_entry', student: @student, entry: @entry) %>");

<!-- Take @child_tasks of @task and find @next_task   -->

<% if @next_task %>
  console.log("<%= @next_task.text %>");
  $('#task<%= @entry.task.id%>').after("<%= escape_javascript render('plans/incomplete_task_entry', task: @next_task) %>"); //Doesn't produce correct task..?

console.log("<%= @next_task.text %>");
<% end %>